'use client';

// Google OAuth is temporarily disabled to prevent redirect_uri mismatch errors on Vercel production.
// NextAuth Google provider remains configured in lib/auth.ts and auth.config.ts.
export default function SocialLoginButtons() {
  return null;
}
